<script lang="ts">
</script>

<main
	class={`bg-dark-neutral-10 block h-screen max-h-screen w-screen overflow-y-auto overflow-x-hidden`}
>
	<header class={`absolute top-0 z-50 flex w-full flex-row items-center justify-center`}>
		<div
			class={`bg-dark-neutral-10 flex h-10 min-h-10 w-full flex-row items-center justify-between bg-opacity-70 backdrop-blur-md`}
		></div>
	</header>
	<section id="hero" class={`flex h-screen w-full max-w-full flex-col items-center justify-start`}>
		<div class={`w-full max-w-screen-md`}>
			<div class={`flex w-full flex-col justify-start items-center`}>
                <!-- profile picture -->
                 <div>
                    
                 </div>
            </div>
		</div>
	</section>
</main>
